const Grocery = () =>{
    return (<div>
        <h2>This is not just a simple component, It contains many components, those components again contain many sub level component</h2>
    </div>)
}

export default Grocery;